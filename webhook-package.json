{
  "name": "shopify-meta-webhook-handler",
  "version": "1.0.0",
  "description": "Capture Shopify conversion data sent to Meta",
  "main": "webhook-handler.js",
  "scripts": {
    "start": "node webhook-handler.js",
    "dev": "nodemon webhook-handler.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

